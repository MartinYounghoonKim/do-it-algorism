function solution(s) {
    generateLength(s);
    var answer = '';
    return answer;
}

function generateLength (s) {
    const stringLength = s.length;
    console.log(stringLength/2);
    if ( stringLength % 2 === 0 ) {

    } else {

    }
}

solution('teste');